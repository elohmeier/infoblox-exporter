version: 2

project_name: infoblox-exporter

builds:
  - id: infoblox-exporter
    main: .
    binary: infoblox-exporter
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.build={{.ShortCommit}}

archives:
  - id: default
    ids:
      - infoblox-exporter
    formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}

checksum:
  name_template: checksums.txt

source:
  enabled: true

snapshot:
  version_template: "{{ incpatch .Version }}-next"

dockers_v2:
  - images:
      - ghcr.io/elohmeier/infoblox-exporter
    tags:
      - "{{.Version}}"
      - latest
    platforms:
      - linux/amd64
      - linux/arm64

release:
  mode: keep-existing
